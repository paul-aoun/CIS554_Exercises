/////////////////////////////////////////////////////
// CIS554 Object Oriented Programming in C++
// Exercise 3_4 - For loops
// Date - 1/15/2020
/////////////////////////////////////////////////////
#include <iostream>
using std::cout;
using std::cin;
using std::endl;
/*
int main()
{
	//Loop to print "* " with additional space at the begining of every other line.
	//variable x is to identify each line, and variable y is to identify every other line.
	for (int x = 1, y = 9; x <= 64; x++, y++)
	{
		
		//Nested if statements to identify a line and every other line
		//Also print "* " for each iteration of the for loop and an additional space for every other line
		if (x % 8 == 0)
		{
			if (y % 16 == 0)
				cout << endl << " "; 
			else
				cout << endl;
		}
		else
		{
			cout << "* ";
		}
	}

	return 0;
}
*/